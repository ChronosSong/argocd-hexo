apiVersion: apps/v1
kind: Deployment
metadata:
  name: hexo-dark
  labels:
    app: hexo-dark
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hexo-dark
  template:
    metadata:
      labels:
        app: hexo-dark
    spec:
      volumes:
        - name: data-hexo
          persistentVolumeClaim:
            claimName: data-hexo-dark
      containers:
        - name: hexo
          image: chronosroot/hexo
          ports:
            - containerPort: 4000
              protocol: TCP
          resources: {}
          volumeMounts:
            - name: data-hexo
              mountPath: /var/www/hexo